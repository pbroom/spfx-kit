# Optional SharePoint workbench host used by imported SPFx apps.
SPFX_SERVE_TENANT_DOMAIN=contoso.sharepoint.com

# Lab defaults. Override when ports are busy.
SPFX_LAB_HOST=127.0.0.1
SPFX_LAB_PORT=5173

# Separate loopback origin and ignored workspace bucket used by the local mock
# CDN. The normal `npm run dev` command starts this service with the Lab.
SPFX_KIT_MOCK_CDN_ORIGIN=http://127.0.0.1:5174
SPFX_KIT_MOCK_CDN_LAB_ORIGIN=http://127.0.0.1:5173
SPFX_KIT_MOCK_CDN_ROOT=.spfx-kit/mock-cdn/v1

# Cloud preview only: provide separate browser-reachable HTTPS forwarders for
# the Lab and mock CDN, then set SPFX_LAB_HOST=0.0.0.0 and
# SPFX_KIT_MOCK_CDN_LISTEN_HOST=0.0.0.0. The public origins must not be
# loopback addresses.
# SPFX_KIT_MOCK_CDN_LAB_ORIGIN=https://lab-preview.example.com
# SPFX_KIT_MOCK_CDN_PUBLIC_ORIGIN=https://cdn-preview.example.com

# Base CDN URL used by `npm run sync:cdn -- --app .spfx-kit/apps/<slug>`.
# Each app resolves to ${SPFX_KIT_CDN_BASE_URL}/<slug>/.
SPFX_KIT_CDN_BASE_URL=https://cdn.example.com/spfx
